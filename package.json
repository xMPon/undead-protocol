{
  "name": "undead-protocol",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Round-based zombie survival shooter for the browser — 3D third-person with a 2D top-down toggle.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
